export class Patterns {
  public static readonly email = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/;
  public static readonly hasAtLeastOneLetter = /[a-zA-Z]/;
  public static readonly hasAtLeastOneNumber = /[0-9]/;
}
